"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.StockCostingService = void 0;
const common_1 = require("@nestjs/common");
const client_1 = require("@prisma/client");
let StockCostingService = class StockCostingService {
    toDecimal(value, fieldName = 'valor') {
        const decimal = new client_1.Prisma.Decimal(value);
        if (!decimal.isFinite()) {
            throw new common_1.BadRequestException(`${fieldName} inválido`);
        }
        return decimal;
    }
    assertPositive(value, fieldName) {
        const decimal = this.toDecimal(value, fieldName);
        if (decimal.lte(0)) {
            throw new common_1.BadRequestException(`${fieldName} deve ser maior que zero`);
        }
        return decimal;
    }
    assertNonNegative(value, fieldName) {
        const decimal = this.toDecimal(value, fieldName);
        if (decimal.lt(0)) {
            throw new common_1.BadRequestException(`${fieldName} não pode ser negativo`);
        }
        return decimal;
    }
    calculateMovingAverageCost(params) {
        const currentQuantity = this.assertNonNegative(params.currentQuantity, 'saldo atual');
        const currentAverageCost = this.assertNonNegative(params.currentAverageCost, 'custo médio atual');
        const incomingQuantity = this.assertPositive(params.incomingQuantity, 'quantidade de entrada');
        const incomingUnitCost = this.assertNonNegative(params.incomingUnitCost, 'custo de entrada');
        const totalQuantity = currentQuantity.plus(incomingQuantity);
        if (totalQuantity.eq(0))
            return new client_1.Prisma.Decimal(0);
        return currentQuantity
            .mul(currentAverageCost)
            .plus(incomingQuantity.mul(incomingUnitCost))
            .div(totalQuantity);
    }
    calculateTotal(quantity, unitCost) {
        const safeQuantity = this.assertPositive(quantity, 'quantidade');
        const safeUnitCost = this.assertNonNegative(unitCost, 'custo unitário');
        return safeQuantity.mul(safeUnitCost);
    }
};
exports.StockCostingService = StockCostingService;
exports.StockCostingService = StockCostingService = __decorate([
    (0, common_1.Injectable)()
], StockCostingService);
//# sourceMappingURL=stock-costing.service.js.map