<ion-content class="ion-padding stock-page">
  <section class="stock-hero">
    <div>
      <p class="eyebrow">Estoque</p>
      <h1><ion-icon name="clipboard-outline"></ion-icon> Inventários</h1>
      <p>Abra contagens por local, registre divergências e gere ajustes auditáveis ao fechar o inventário.</p>
    </div>
    <ion-button fill="outline" (click)="load()"><ion-icon name="refresh-outline" slot="start"></ion-icon>Atualizar</ion-button>
  </section>

  <section class="stock-card stock-form-grid">
    <ion-item>
      <ion-label position="stacked">Local de estoque</ion-label>
      <ion-select [(ngModel)]="form.localEstoqueId" interface="popover" placeholder="Selecione o local">
        <ion-select-option *ngFor="let location of locations" [value]="location.id">{{ location.nome }} ({{ location.codigo }})</ion-select-option>
      </ion-select>
    </ion-item>
    <ion-item>
      <ion-label position="stacked">Observação</ion-label>
      <ion-textarea [(ngModel)]="form.observacao" placeholder="Ex.: inventário mensal do almoxarifado"></ion-textarea>
    </ion-item>
    <ion-button (click)="create()" [disabled]="saving || !form.localEstoqueId"><ion-icon name="save-outline" slot="start"></ion-icon>Abrir inventário</ion-button>
  </section>

  <ion-searchbar [(ngModel)]="search" (ionInput)="load()" placeholder="Buscar por local ou observação"></ion-searchbar>
  <ion-spinner *ngIf="loading"></ion-spinner>

  <section class="stock-table" *ngIf="!loading">
    <table>
      <thead>
        <tr><th>Local</th><th>Status</th><th>Abertura</th><th>Itens</th><th>Ações</th></tr>
      </thead>
      <tbody>
        <tr *ngFor="let inventory of inventories">
          <td>{{ inventory.localEstoque?.nome || '-' }}</td>
          <td><span class="stock-badge">{{ inventory.status }}</span></td>
          <td>{{ inventory.dataAbertura | date:'short' }}</td>
          <td>{{ inventory._count?.itens || inventory.itens?.length || 0 }}</td>
          <td><ion-button size="small" fill="outline" [routerLink]="['/stock/inventories', inventory.id]">Abrir</ion-button></td>
        </tr>
      </tbody>
    </table>
  </section>
</ion-content>
