<ion-content class="stock-content"><div class="stock-page"><header class="stock-header"><div><h1>Locais de estoque</h1><p>Depósitos, almoxarifados, canteiros e estoques de obra</p></div><ion-button (click)="reset()"><ion-icon name="add-outline" slot="start"></ion-icon>Novo</ion-button></header><div class="loading" *ngIf="loading"><ion-spinner name="crescent"></ion-spinner><p>Carregando locais...</p></div><section class="form-panel" *ngIf="!loading"><div class="panel-title"><ion-icon name="business-outline"></ion-icon><h2>{{ form.id ? 'Editar local' : 'Cadastrar local' }}</h2></div><div class="form-grid"><ion-item><ion-label position="stacked">Código *</ion-label><ion-input [(ngModel)]="form.codigo"></ion-input></ion-item><ion-item class="wide"><ion-label position="stacked">Nome *</ion-label><ion-input [(ngModel)]="form.nome"></ion-input></ion-item><ion-item><ion-label position="stacked">Tipo *</ion-label><ion-select [(ngModel)]="form.tipo"><ion-select-option *ngFor="let type of types" [value]="type">{{ type }}</ion-select-option></ion-select></ion-item><ion-item class="wide"><ion-label position="stacked">Endereço/descrição</ion-label><ion-textarea [(ngModel)]="form.endereco"></ion-textarea></ion-item><ion-item><ion-label>Permite saldo negativo</ion-label><ion-toggle [(ngModel)]="form.permiteSaldoNegativo"></ion-toggle></ion-item><ion-item><ion-label>Ativo</ion-label><ion-toggle [(ngModel)]="form.ativo"></ion-toggle></ion-item></div><div class="form-actions"><ion-button fill="outline" (click)="reset()">Limpar</ion-button><ion-button (click)="save()" [disabled]="saving || !form.codigo || !form.nome"><ion-icon name="save-outline" slot="start"></ion-icon>Salvar</ion-button></div></section><section class="panel" *ngIf="!loading"><div class="toolbar-row"><ion-searchbar [(ngModel)]="search" placeholder="Buscar local" (ionInput)="load()"></ion-searchbar><ion-button fill="outline" (click)="load()"><ion-icon name="refresh-outline" slot="start"></ion-icon>Atualizar</ion-button></div><table><thead><tr><th>Código</th><th>Nome</th><th>Tipo</th><th>Obra</th><th>Saldo negativo</th><th>Status</th></tr></thead><tbody><tr *ngFor="let item of locations" (click)="edit(item)"><td>{{ item.codigo }}</td><td>{{ item.nome }}</td><td>{{ item.tipo }}</td><td>{{ item.obra?.nome || '-' }}</td><td>{{ item.permiteSaldoNegativo ? 'Sim' : 'Não' }}</td><td><span class="status" [class.critical]="!item.ativo">{{ item.ativo === false ? 'INATIVO' : 'ATIVO' }}</span></td></tr></tbody></table><div class="empty" *ngIf="!locations.length">Nenhum local encontrado.</div></section></div></ion-content>
