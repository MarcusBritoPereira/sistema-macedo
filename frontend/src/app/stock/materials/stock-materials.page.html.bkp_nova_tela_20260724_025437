<ion-content class="stock-content"><div class="stock-page">
<header class="stock-header"><div><h1>Materiais</h1><p>Catálogo central de insumos e materiais da construção civil</p></div><ion-button (click)="reset()"><ion-icon name="add-outline" slot="start"></ion-icon>Novo</ion-button></header>
<div class="loading" *ngIf="loading"><ion-spinner name="crescent"></ion-spinner><p>Carregando materiais...</p></div>
<section class="form-panel" *ngIf="!loading"><div class="panel-title"><ion-icon name="cube-outline"></ion-icon><h2>{{ form.id ? 'Editar material' : 'Cadastrar material' }}</h2></div><div class="form-grid">
<ion-item><ion-label position="stacked">Código *</ion-label><ion-input [(ngModel)]="form.codigo"></ion-input></ion-item>
<ion-item class="wide"><ion-label position="stacked">Nome *</ion-label><ion-input [(ngModel)]="form.nome"></ion-input></ion-item>
<ion-item><ion-label position="stacked">Unidade *</ion-label><ion-select [(ngModel)]="form.unidade"><ion-select-option *ngFor="let unit of units" [value]="unit">{{ unit }}</ion-select-option></ion-select></ion-item>
<ion-item class="wide"><ion-label position="stacked">Categoria *</ion-label><ion-select [(ngModel)]="form.categoriaMaterialId"><ion-select-option *ngFor="let cat of categories" [value]="cat.id">{{ cat.nome }}</ion-select-option></ion-select></ion-item>
<ion-item><ion-label position="stacked">Estoque mínimo</ion-label><ion-input type="number" [(ngModel)]="form.estoqueMinimo"></ion-input></ion-item>
<ion-item><ion-label position="stacked">Ponto reposição</ion-label><ion-input type="number" [(ngModel)]="form.pontoReposicao"></ion-input></ion-item>
<ion-item><ion-label position="stacked">Custo padrão</ion-label><ion-input type="number" [(ngModel)]="form.custoPadrao"></ion-input></ion-item>
<ion-item><ion-label position="stacked">Marca</ion-label><ion-input [(ngModel)]="form.marca"></ion-input></ion-item>
<ion-item><ion-label position="stacked">Código de barras</ion-label><ion-input [(ngModel)]="form.codigoBarras"></ion-input></ion-item>
<ion-item><ion-label>Fracionado</ion-label><ion-toggle [(ngModel)]="form.permiteFracionado"></ion-toggle></ion-item>
<ion-item><ion-label>Ativo</ion-label><ion-toggle [(ngModel)]="form.ativo"></ion-toggle></ion-item>
<ion-item class="wide"><ion-label position="stacked">Observações</ion-label><ion-textarea [(ngModel)]="form.observacoes"></ion-textarea></ion-item>
</div><div class="form-actions"><ion-button fill="outline" (click)="reset()">Limpar</ion-button><ion-button (click)="save()" [disabled]="saving || !form.codigo || !form.nome || !form.categoriaMaterialId"><ion-icon name="save-outline" slot="start"></ion-icon>Salvar</ion-button></div></section>
<section class="panel" *ngIf="!loading"><div class="toolbar-row"><ion-searchbar placeholder="Buscar por código, nome, marca..." [(ngModel)]="search" (ionInput)="load()"></ion-searchbar><ion-button fill="outline" (click)="load()"><ion-icon name="refresh-outline" slot="start"></ion-icon>Atualizar</ion-button></div><table><thead><tr><th>Código</th><th>Material</th><th>Categoria</th><th>Un.</th><th>Saldo</th><th>Valor</th><th>Status</th></tr></thead><tbody><tr *ngFor="let item of materials" (click)="edit(item)"><td>{{ item.codigo }}</td><td>{{ item.nome }}</td><td>{{ item.categoriaMaterial?.nome || '-' }}</td><td>{{ item.unidade }}</td><td>{{ item.saldoTotal || 0 | number:'1.2-2' }}</td><td>{{ item.valorTotalEstoque || 0 | currency:'BRL':'symbol':'1.2-2':'pt-BR' }}</td><td><span class="status" [class.critical]="!item.ativo">{{ item.ativo === false ? 'INATIVO' : 'ATIVO' }}</span></td></tr></tbody></table><div class="empty" *ngIf="!materials.length">Nenhum material encontrado.</div></section>
</div></ion-content>
